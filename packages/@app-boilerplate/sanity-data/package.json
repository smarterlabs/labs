{
  "name": "@app-boilerplate/sanity-data",
  "private": true,
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx dotenv -e ../../../.env -e ../.env -- node fetch",
    "dev": "npx dotenv -e ../../../.env -e ../.env -- node fetch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sanity/client": "^0.142.0",
    "fs-extra": "^8.1.0"
  },
  "devDependencies": {
    "dotenv-cli": "^3.0.0"
  }
}
