{
  "name": "@app-boilerplate/sanity-data",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "npx dotenv -e ../../../.env -- node fetch",
		"dev": "npx dotenv -e ../../../.env -- node fetch",
		"clean": "rm -rf dist"
  },
  "dependencies": {
    "@sanity/client": "^0.142.0",
    "fs-extra": "^8.1.0"
  }
}
